# HUD 重设计方案

> 基于原版PT UI的忠实还原 + 可配置键位系统

## 1. 设计目标

- 忠实还原原版PT HUD的视觉风格和布局
- 按16:9比例（1280×720）调整尺寸和间距
- 新增可配置键位系统

## 2. HUD布局（1280×720）

### 2.1 原版元素映射

原版800×600坐标系，按比例缩放至1280×720：

| 原版元素 | 原版位置(800×600) | 新位置(1280×720) | 说明 |
|----------|------------------|------------------|------|
| Menu-1背景 | (288,472) 256×128 | (461,566) 410×128 | 底部中央背景 |
| Menu-2背景 | (544,536) 256×64 | (871,643) 410×64 | 底部中央背景 |
| HP条 | (319,500) 16×94 | (511,600) 16×94 | 红色，底部锚定 |
| MP条 | (465,500) 16×94 | (744,600) 16×94 | 蓝色，底部锚定 |
| STM条 | (303,518) 8×76 | (485,622) 8×76 | 绿色，底部锚定 |
| 左技能槽 | (338,542) 16×41 | (541,651) 16×41 | 左键技能 |
| 右技能槽 | (446,542) 16×41 | (714,651) 16×41 | 右键技能 |
| EXP条 | (485,508) 6×86 | (776,610) 6×86 | 黄色，底部锚定 |
| 日月时钟 | (363-426,589) 13×13 | (581-682,707) 13×13 | 太阳/月亮 |
| 时条 | (375,593) 50×5 | (600,712) 50×5 | 时间条 |
| Walk按钮 | (575,565) 24×25 | (920,678) 24×25 | 走路/跑步 |
| 相机按钮 | (599,565) 24×25 | (958,678) 24×25 | 相机模式 |
| 地图按钮 | (623,565) 24×25 | (997,678) 24×25 | 小地图开关 |
| 6功能按钮 | (648-772,560) 各25×27 | (1037-1235,670) 各25×27 | 状态/背包/技能/组队/任务/系统 |
| 小地图 | (656,426) 128×128 | (1050,511) 128×128 | 右上角 |

### 2.2 缩放策略

- 画布固定1280×720逻辑像素
- CSS缩放适配视口：`scale = min(viewportW/1280, viewportH/720)`
- 画布居中，两侧/上下留黑边
- DPR感知：canvas实际像素 = 逻辑像素 × DPR

### 2.3 渲染层级

```
1. 清屏
2. 底部HUD背景（Menu-1/Menu-2）
3. HP/MP/STM/EXP条
4. 技能槽（左右）
5. 日月时钟
6. Walk/相机/地图按钮
7. 6功能按钮
8. 小地图
9. 3D场景
10. 浮动文字/伤害数字
```

## 3. 可配置键位系统

### 3.1 功能列表

保持原版默认键位，WASD移动作为可选配置。

| 操作 | 默认键 | 说明 |
|------|--------|------|
| 移动前 | ArrowUp | 向前移动 |
| 移动后 | ArrowDown | 向后移动 |
| 移动左 | ArrowLeft | 向左移动 |
| 移动右 | ArrowRight | 向右移动 |
| 攻击 | Space | 左键攻击/自动拾取 |
| 技能 | Control | 右键攻击/右手技能 |
| 走路/跑步 | R | 切换移动模式 |
| 相机模式 | Z | 切换相机 |
| 小地图 | TAB | 切换小地图 |
| 角色状态 | C | 打开角色面板 |
| 技能面板 | S | 打开技能面板 |
| 背包面板 | V | 打开背包面板 |
| 组队窗口 | D | 打开组队窗口 |
| 任务窗口 | Q | 打开任务窗口 |
| 系统菜单 | X | 打开系统菜单 |
| 显示地面物品 | A | 切换地面物品名称显示 |
| 技能1-12 | F1-F12 | 主动作条 |
| 药水1-12 | 1-0,-,= | 副动作条 |
| 聊天输入 | Enter | 打开聊天 |
| 关闭面板 | Escape | 关闭当前面板 |

### 3.2 补充槽位

以下操作默认不绑定按键，用户可在设置中自行绑定：
- 移动前
- 移动后
- 移动左
- 移动右

### 3.2 存储方案

- 存储位置：localStorage `pt-keybindings`
- 数据格式：JSON `{ "moveForward": "ArrowUp", "attack": "Space", ... }`
- 加载时机：游戏初始化时从localStorage读取
- 未配置的操作使用默认值

### 3.3 设置面板

- 入口：X系统菜单 → "键位设置"按钮
- 布局：列表形式，左侧操作名称，右侧当前绑定键
- 交互：点击按键显示"请按键..."，按下新键后绑定
- 按钮：重置默认（恢复原版键位）、保存、取消

## 4. 时间同步与昼夜系统

### 4.1 时间同步协议

采用混合方案：初始同步 + 本地推进 + 定期校正。

**初始同步（连接时）：**
- 服务端发送 `OpCode::Connected`，附带当前游戏时间（毫秒）
- 客户端接收后转换为游戏分钟：`dwGameWorldTime = dwTime / GAME_WORLDTIME_MIN`
- 存储 `dwConnectedServerTime` 和 `dwConnectedClientTime` 用于后续校正

**本地推进：**
- 客户端按800ms = 1游戏分钟自行推进时间
- `dwGameHour = dwGameWorldTime / 60 % 24`
- `dwGameMin = dwGameWorldTime % 60`

**定期校正（每4秒）：**
- 客户端发送 `OpCode::CheckNetState`，附带 `计算的服务器时间 = (本地时间 - 连接时客户端时间) + 连接时服务端时间`
- 服务端回传当前时间
- 客户端计算漂移，偏差>10分钟时强制校正，否则允许小范围漂移

**相关文件：**
- netplay.cpp:1672-1679（初始同步）
- netplay.cpp:5471-5585（心跳对时）
- OnSever.cpp:19801-19862（服务端响应）

### 4.2 游戏时间系统

| 常量 | 值 | 含义 |
|------|-----|------|
| GAME_WORLDTIME_MIN | 800 | 800ms真实时间 = 1游戏分钟 |
| GAME_HOUR_DAY | 4 | 黎明开始 |
| GAME_HOUR_GLOW | 22 | 日落开始 |
| GAME_HOUR_DARKNESS | 23 | 完全黑暗 |
| GAME_HOUR_NIGHT | 0 | 深夜 |

**昼夜循环：**
```
深夜(0-3) → 黎明(4) → 白天(4-21) → 日落(22) → 黑暗(23) → 深夜(0)
```

### 4.3 客户端渲染

**天空背景切换：**
- 每个场地有3套天空贴图：`BackImageCode[0]`=日，`[1]`=暮，`[2]`=夜
- 按 `dwGameHour` 选择：4-21=日，22=暮，23-3=夜
- 天空切换时平滑交叉淡入淡出（HoSky.cpp:696-753）

**暗度系统（DarkLevel）：**
- `DarkLight` 为天空配置的目标值
- `DarkLevel` 每4帧向目标值插值±1（playmain.cpp:2412-2416）
- 用于动态灯光颜色：`lightRGB * DarkLevel / 256`

**动态灯光：**
- 带 `smLIGHT_TYPE_NIGHT` 标记的灯光仅夜间渲染
- 其他灯光始终渲染，但受 DarkLevel 调色

**日月时钟HUD：**
- 8-18点显示太阳图标，其余显示月亮
- 时条按昼夜进度填充：
  - 白天：`fill = 50 * (hour-4) / 19`（从左到右）
  - 夜晚：`fill = 50 * (hour+1) / 5`（从右到左）

### 4.4 服务端逻辑

- 每1024 tick重新计算昼夜状态（OnSever.cpp:8359-8366）
- 夜间替换NPC为怪物（噩梦事件字段）
- 控制事件怪物刷新（字段代码3，夜间刷 code 100 怪物）
- 村庄内 DarkLevel 减半（character.cpp:6861-6863）

### 4.5 客户端实现清单

- [ ] 接收初始时间同步，存储偏移量
- [ ] 本地时间推进（每帧累加 deltaTime）
- [ ] 4秒心跳发送计算的服务器时间
- [ ] 漂移校正（>10分钟强制，否则平滑）
- [ ] 按小时计算昼夜状态（NightDayTime）
- [ ] 天空背景切换（日/暮/夜）
- [ ] DarkLevel 平滑插值
- [ ] 动态灯光夜间过滤
- [ ] 日月时钟渲染

## 5. 实现优先级

### P0 — 核心
- [x] 1280×720画布 + CSS缩放（实现见 §5.1：底部锚定 + 焦点居中）
- [x] 加载并渲染原版HUD纹理（bar_life, bar_mana, bar_stamina, bar_exp等）
- [x] 条的绘制（垂直填充，底部锚定）
- [x] 日月时钟渲染
- [x] 小地图渲染（实现见 §5.4：统一世界窗口多图同绘 + i18n 文本标题）
- [x] 服务端对时协议实现（实现见 §5.2：服务器权威单调时钟）
- [x] 客户端时间同步与本地推进
- [x] 昼夜状态计算（NightDayTime）
- [ ] 天空背景切换（日/暮/夜）—— **未完成，挂起**（需 sky.txt + SMD 广告牌层，独立排期）
- [x] DarkLevel 平滑插值

### P1 — 交互
- [ ] 按钮点击检测（hit test）
- [x] 键盘快捷键绑定系统（KeyBinding.ts，原版默认键，见 §3）
- [x] 键位设置面板UI（KeyBindingPanel.ts，X 系统菜单弹出）
- [x] localStorage读写（KeyBinding.ts）
- [x] Walk/相机/地图按钮渲染
- [x] 6功能按钮渲染

### P2 — 完善
- [ ] 技能槽（左右键）
- [ ] 鼠标悬停提示（HP/MP/STM数值）
- [ ] EXP百分比文本
- [ ] 动态灯光夜间过滤
- [ ] 昼夜NPC切换（服务端）

### P3 — 扩展
- [ ] 面板系统（C/S/V）
- [ ] 战斗指示器（防御/格挡/闪避）
- [ ] 命中计数板
- [ ] 伤害飘字

## 6. P0 实现结论（2026-09-02）

> 本轮实现偏离 §2.1 的"缩放至1280×720新坐标"设想，实际改为：**按原版 800×600 坐标绘制 + 图层平移定位**。以下为落地结论，与上文章节相抵触处以本节省略为准。

### 6.1 HUD 画布与定位（Hud.ts）
- 画布固定 1280×720；内容仍用原版 800×600 坐标绘制。
- **水平**：以玩家感知焦点 = HP/MP 条与左右技能图标中点（原版 x≈400）对齐画布中心 640 ⇒ `ctx.translate(240, 120)`。
- **垂直**：内容底（原版 y=600）贴画布底 720（底锚定）。`fitCanvas` 等比 `min(vw/1280, vh/720)`、CSS 底部锚定，小窗口时等比缩小（不做统一 letterbox；devtools 极端视口属已知可接受噪音）。
- Menu 背景 = `menu-1.tga`/`menu-2.tga`（TGA 带 alpha）；bar_life/bar_mana/bar_stamina 是自下而上填充的**填充条**（无独立背景框，原版 Shadow* 为死代码）。
- 技能区默认画两个拳头 `skill/skill_normal.bmp` @ (349,541)/(403,541) 49×46；药水槽底图 `inven/potionback.bmp` @(495,565)。
- `inter_01/02/03` 本客户端为纯黑占位 → **删除绘制**（原版用途是宽屏时把底条延伸到 800px 之外，16:9 下无存在价值，spec §2.1 表已删）。

### 6.2 服务端对时（权威时钟）
- 服务端 `PingService.pong.timestamp` = **服务器单调毫秒**（`nanoTime` 模拟原版 `GetTickCount`），非回显客户端值。
- 客户端换算 `gameMin = ms/800`（GAME_WORLDTIME_MIN=800），`hour=(gameMin/60)%24`，本地每 800ms 推进，漂移 >10 游戏分钟 snap 回（原版 `abs(dwGameWorldTime-dwTime)>10`；此前误用 600000ms 墙钟）。
- 连接即发一次 ping 做首同步；`GameClock.isSynced()` 区分首次锚定/周期校正。

### 6.3 坐标系（统一 raw→GL，权威 = pt-game-server loader/RenderMapPng）
- smd 顶点分量0=东(A)、分量2=北(C)（`StageVertex` 读取：`(x,y,z)=(-C,B,-A)/256`）。
- **GL 目标：东=+X、北=−Z** ⇒ `wx=+A/256, wz=−C/256`；`rawToWorld=(x,y,-z)`。
- 服务端始终下发原始坐标（东 x、北 z），startPoint 无需改；此前客户端 `(-z,y,-x)` 消费导致 90° 错位，已全量修正（renderer 顶点/bounds/lights、fore1 bounds、移动/碰撞/掉落往返、findCurrentMap、装饰整体绕 Y+90°）。
- 服务端 startPoint/门/center 与 smd header RECT 同一原始坐标系（+x 东/+z 北），跨图连续。

### 6.4 昼夜（忠实原版）
- 驱动 = 每帧 `dnUpdate`（移植 /pt/maps index.html）：`DarkLevel`/`BackColor` ±1 渐变趋向时段目标，环境光偏移 `(-DarkLevel+BackColor)/255` 经 shader uniform（`uEnvLight`/`uSceneLight[8]`/`uTorch`）加到预烘焙顶点色（MeshBasicMaterial+vertexColors，**不受 THREE 灯光影响**——改 Ambient/Dir 无效，这是最初"乱来"的根因）。
- 附加：玩家火把（ap=Dark×1.25、范围 260）、玩家附近 ≤8 场景灯（NIGHT 型全亮、其余 ×DarkLevel>>8）。
- 时段槽（户外）：白昼 dark=1 /back(0,0,-10)，傍晚(22h)=24/(28,0,-30)，夜(23-3h)=145/(-50,0,10)。
- **按地图档案 `map-light.ts`**：地牢/室内(13-16,22-26,32,36,40-43) 恒夜固定 DarkLevel/BackColor（HoSky MainSky 表：110 纯黑 / dun-4,5=40 黑 / tcave=60 蓝 / mcave=60 冷 / dcave+endless=40 蓝）；村庄(3,21) 夜间地形色 >>1；其余按小时昼夜。
- 时间由 GameClock 喂 `WorldView.setGameTime(hour,min)`。

### 6.5 小地图（忠实原版 playsub.cpp，浮点移植）
- 机制：以玩家为中心的**统一世界窗口**（半宽 = 地牢16/普通24 格 ×64 world 单位），遍历所有已加载图（当前+邻图）把自己落在窗口内的部分**裁剪进同一 126 盒** → 交界两图同时可见且无缝（对齐原版双 stage 语义）。
- 北 = −Z 上、东 = +X 右；箭头恒在框中心旋转 yaw（`scale(-1,1)` 修正贴图轴向）。TAB 开关（默认开）；M/双击全屏图后续。
- 标题**不用 `<name>t.tga` 贴图**，走 i18n `t('map.<mapId>')`（locales zh/en）。
- 资源：`field/map/<ase>.tga`（整场俯视图）+ `Image/arrow.tga mapbox.tga`。

### 6.6 调试便利
- 左下角面板：FPS/Draw/Tris/Verts + `Pos (x,y,z) m<mapId>` + `Time HH:mm`(调试小时带*) + `Dark n`。
- `[`/`]` 临时调游戏小时（`DN_DEBUG_KEYS` 集中标注，后期删除）。
- 顶 P0 清单与 §2.3 布局映射按实际调整；服务端对时字段/协议不变。
